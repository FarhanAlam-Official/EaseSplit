# ============================================
# EaseSplit - Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values
# ============================================

# ============================================
# SMTP Configuration (Required for sending emails)
# ============================================
# Default SMTP settings used when "Default SMTP" is selected in the app
# If using Gmail, create an App Password: https://support.google.com/accounts/answer/185833

# SMTP Server Host
# Examples:
#   - Gmail: smtp.gmail.com
#   - Outlook: smtp.office365.com
#   - Custom: smtp.yourdomain.com
SMTP_HOST=smtp.gmail.com

# SMTP Server Port
# Common ports:
#   - 587 (TLS - Recommended)
#   - 465 (SSL)
#   - 25 (Unencrypted - Not recommended)
SMTP_PORT=587

# SMTP Authentication Username (usually your email address)
SMTP_USER=your-email@gmail.com

# SMTP Authentication Password
# For Gmail: Use an App Password (not your regular password)
# For other providers: Use your email account password or app-specific password
SMTP_PASSWORD=your-app-password-here

# SMTP From Address (Optional)
# Format: Display Name <email@domain.com>
# If not set, defaults to SMTP_USER
SMTP_FROM=EaseSplit <noreply@easesplit.com>

# SMTP Secure Connection (Optional)
# Set to "true" if using SSL/TLS encryption (port 465 typically requires this)
# Set to "false" or omit for STARTTLS (port 587 typically uses this)
SMTP_SECURE=false

# ============================================
# Admin Configuration
# ============================================
# Email address for receiving contact form submissions and system notifications
ADMIN_EMAIL=your-email@gmail.com

# ============================================
# Optional: Additional Configuration
# ============================================
# Uncomment and configure these if needed:

# Node Environment
# NODE_ENV=production

# Application URL (for certificate links, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000


# EaseSplit Website URL Configuration
# These variables control the EaseSplit website link displayed in email footers
# The system checks them in the order listed below

# Primary: Public EaseSplit URL (exposed to client-side, recommended for most cases)
# This URL will be used in email footers and any client-facing links
NEXT_PUBLIC_EASESPLIT_URL=https://easesplit.vercel.app

# Alternative: Server-side only EaseSplit Website URL
# Use this if you prefer to keep the URL server-side only (not exposed to client)
EASESPLIT_WEBSITE_URL=https://easesplit.vercel.app

# Base URL Configuration (alternative fallback options)
# These are checked if the above variables are not set
# NEXT_PUBLIC_BASE_URL is client-accessible, BASE_URL is server-only
NEXT_PUBLIC_BASE_URL=https://easesplit.vercel.app
BASE_URL=https://easesplit.vercel.app

# Note: If none of the above are set, the system will fallback to:
# https://easesplit.vercel.app

# ============================================
# Notes:
# ============================================
# 1. Never commit .env.local to version control (it's in .gitignore)
# 2. For production, set these variables in your hosting platform:
#    - Vercel: Project Settings > Environment Variables
#    - Netlify: Site Settings > Environment Variables
#    - Railway: Variables tab
# 3. The app supports both default SMTP (from env vars) and custom SMTP (entered per-send)
# 4. Bill splitting features work entirely client-side and don't require any env vars

